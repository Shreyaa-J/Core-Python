def reversal(n,rev):
    if n <= 0:
        return rev
    rem=n%10
    rev=(rev*10)+rem
    n=n//10
    return reversal(n,rev)

num=int(input("Enter Number : "))
res=reversal(num,0)
print(f"The reversal of {num} is : {res}")
